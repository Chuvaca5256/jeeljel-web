# API-Sports — misma key que en el VPS (NO commitear valores reales)
API_SPORTS_KEY=

# Redis
REDIS_URL=redis://localhost:6379

# Polling — 600000 = 10 min (plan FREE). Cambiar a 60000 en PRO, 15000 en eventos de alta demanda.
POLLING_INTERVAL_MS=600000

# Puerto del servicio Ollin — NUNCA 10000 (reservado para Ikan Naat)
OLLIN_PORT=10001
