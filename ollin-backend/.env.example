# API-Sports — misma key que en el VPS (NO commitear valores reales)
API_SPORTS_KEY=

# Redis
REDIS_URL=redis://localhost:6379

# Polling — FREE=600000 (10 min) · PRO=60000 (60 s) · PRO con torneo selecciones activo=15000 (15 s)
POLLING_INTERVAL_MS=600000

# Puerto del servicio Ollin — NUNCA 10000 (reservado para Ikan Naat)
OLLIN_PORT=10001

# Supabase — chat en vivo y moderación (service role, solo backend)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
